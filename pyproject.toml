[project]
name        = "delusion"
description = "✨ A minimal generative models router"
authors     = [{name="Tremeschin"}]
version     = "0.1.0"
readme      = "readme.md"
license     = "MIT"
requires-python = ">=3.10"
dependencies = [
    "cachetools",
    "ollama",
    "diskcache",
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
